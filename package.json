{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "spago build",
    "test": "spago -x test/test.dhall test"
  },
  "devDependencies": {
    "purescript-psa": "^0.7.3",
    "rimraf": "^3.0.2",
    "spago": "^0.15.3"
  }
}
